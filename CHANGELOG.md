# 更新日志

## [1.1.0] — 2026-05-11

### 变更

- **界面主题**：整体由深色改为**浅色主色**（背景、侧栏、顶栏、卡片与表格样式统一为浅色系）。
- **表单可读性**：修正 `form-select` / `form-control` 在浅色背景下对比度不足的问题；下拉框与选项为**白底深色文字**，并设置 `color-scheme: light` 以改善系统原生下拉在 Windows 等环境下的显示。
- **模板**：将部分标题由 `text-white` 改为 `text-dark`，避免在浅色卡片上出现白字不可读（工作台、课程列表、选课页、批量考勤页）。

### 说明

- 业务逻辑与路由未改动，升级后无需迁移数据库。

---

## [1.0.0] — 初始版本

- Flask 学生管理系统：学生、班级、课程、选课、成绩、考勤模块；SQLite；Flask-Login；README 与 Git 仓库初始化。
